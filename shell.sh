# Common commands

# head -c 32 /dev/urandom | base64 # generate 32 characters random string
# usage: used for generating a random secret string !!!!

# echo "SECREST_HASH=" | base64 --decode # decode a secret !!

# cat /etc/*release* # check what os on the machines?

# ps -ef | grep kube-apiserver | grep admission-plugins # To see all admissions

