appVersion: v1
kind: Pod
metadata:
  name: app-dashboard
spec:
  containers:
    - name: app-dashboard
      image: app-dashboard
  serviceAccountName: dashboard-sa


# Common commands

# kubectl create serviceaccount san
# kubectl describe serviceaccount san
# kubectl create token san # create a token for san named service account

# Why we need service account?

# service account is used by kubernetes pods applications to interact kubernete clusters 

# jenkins can use service account to deploy app to lubernete cluster !!!!!